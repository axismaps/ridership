{
  "name": "ridership",
  "version": "1.0.0",
  "description": "Ridership Visualization",
  "main": "index.js",
  "scripts": {
    "data:clean": "python bin/ntd.py",
    "data:tiles": "mapshaper data/geojson/**/routes.geojson combine-files -merge-layers force -filter-fields color,high_frequency,name,operated_by_name,tags,vehicle_type -o data/output/routes.geojson && mapshaper data/geojson/**/stops.geojson combine-files -merge-layers force -filter-fields high_frequency,name,tags -o data/output/stops.geojson && tippecanoe data/output/*.geojson -aD -aG -ab -ai -f -o data/output/transit.mbtiles",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/axismaps/ridership.git"
  },
  "author": "Axis Maps",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/axismaps/ridership/issues"
  },
  "homepage": "https://github.com/axismaps/ridership#readme",
  "devDependencies": {
    "mapshaper": "^0.4.99"
  }
}
